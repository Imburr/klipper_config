[gcode_macro STATS_RESET]
description: Calling the MMU_STATS RESET=1 macro from Mainsail.
gcode:
  MMU_STATS RESET=1

[gcode_macro GATEMAP_RESET]
description: Calling the MMU_REMAP_TTG RESET= macro from Mainsail.
gcode:
  MMU_REMAP_TTG RESET=1